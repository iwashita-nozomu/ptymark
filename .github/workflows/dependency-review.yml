# @dependency-start
# contract workflow
# responsibility Blocks pull requests that introduce dependencies with known high-or-critical vulnerabilities.
# upstream configuration ../dependabot.yml proposes dependency updates
# upstream environment ../../Cargo.lock resolved Rust dependency graph
# upstream environment ../../renderers/package-lock.json resolved renderer dependency graph
# downstream policy ../../SECURITY.md vulnerability reporting and support policy
# @dependency-end

name: Dependency Review

on:
  pull_request:
    branches:
      - main
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "pyproject.toml"
      - "docker/requirements.txt"
      - "docker/Dockerfile"
      - "docker/**/Dockerfile"
      - "docker/*.Dockerfile"
      - "renderers/package.json"
      - "renderers/package-lock.json"
      - ".gitmodules"
      - ".github/dependabot.yml"
      - ".github/workflows/**"

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: dependency-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  dependency-review:
    name: Dependency vulnerability review
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Review dependency changes
        uses: actions/dependency-review-action@219bfff8dc43b986fdc35d4f24b3e92d33fd0c88 # v4
        with:
          fail-on-severity: high
