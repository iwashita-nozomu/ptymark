# @dependency-start
# contract template
# responsibility Defines default template experiment configuration.
# upstream design README.md experiment topic template
# downstream implementation run.py reads generated run config that includes this file.
# @dependency-end
mode: template
limit: 8
