# ptymark

このリポジトリをテンプレートとしてクローンして開始するには、GitHub CLI を使うのが簡単です。

```bash
gh repo create <new-repo-name> --template iwashita-nozomu/ptymark --clone
cd <new-repo-name>
```

GitHub CLI を使わない場合は、通常の clone 後に `origin` を新しいリポジトリへ差し替えて利用してください。